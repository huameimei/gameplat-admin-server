spring:
  application:
    name: gameplat-admin-server
  profiles:
    active: @profiles.active@
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: 192.168.12.50:8848,192.168.12.50:8848
        username: nacos
        password: t1KMNoXowYr2DK9L
        file-extension: yml
        namespace: prod
        group: DEFAULT_GROUP
        refresh-enabled: true
        shared-dataids: redis.yml,spring-kafka-producer-config.yml
      discovery:
        service: ${USERDOMAIN}-admin-server
        namespace: ${spring.cloud.nacos.config.namespace}
        group: SERVICE_GROUP
        server-addr: ${spring.cloud.nacos.config.server-addr}
        username: ${spring.cloud.nacos.config.username}
        password: ${spring.cloud.nacos.config.password}
