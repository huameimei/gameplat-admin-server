spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: admin-server
  profiles:
    active: @profiles.active@
  websocket:
    strategy: redis
    endpoint: /websocket
    broker: /topic1,/user
    heartbeat: 10000,10000
    userDestinationPrefix: /user
    applicationDestinationPrefixes: /app
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: 192.168.15.244:8848,192.168.15.245:8848,192.168.15.246:8848
        username: nacos
        password: t1KMNoXowYr2DK9L
        file-extension: yml
        namespace: prod-${profile}
        group: SERVICE_GROUP
        refresh-enabled: true
        shared-configs:
          - data-id: redis.yml
            refresh: true
          - data-id: injection-http-firewall.yml
            refresh: true
          - data-id: spring-kafka-log.yml
            refresh: true
          - data-id: spring-kafka-producer-config.yml
            refresh: true
          - data-id: elasticsearch.yml
            refresh: true
      discovery:
        service: admin-server
        namespace: prod
        group: SERVICE_GROUP
        server-addr: ${NACOS_SERVER_ADDR:localhost}
        username: nacos
        password: t1KMNoXowYr2DK9L
